# Quests screen uplift prompt

You are editing **ONLY** the file: `src/screens/quests/uplift.css`.

Rules:
- Output the **full updated CSS file only**.
- Do **not** suggest changes to JS/HTML.
- Scope **every selector** under `.questsScreen`.
- Do not target global selectors (`body`, `html`, `.screen-root`, `#plane`).
- Do not change or assume the router gesture system; avoid `touch-action` changes.

Available hooks:
- Screen root: `.questsScreen`
- Scroll container: `.questsScroll`

Intent:
- Layout polish: spacing, typography rhythm, section padding, card density, etc.
