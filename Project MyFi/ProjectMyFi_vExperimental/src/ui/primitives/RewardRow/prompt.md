# RewardRow uplift prompt

You are editing **ONLY** the file: `src/ui/primitives/RewardRow/uplift.css`.

Rules:
- Output the **full updated CSS file only**.
- Do **not** suggest changes to JS/HTML.
- Scope **every selector** under `.RewardRowRoot`.
- Do not target global selectors (`body`, `html`, `.screen-root`, `#plane`).

Available hooks:
- Root: `.RewardRowRoot`
- Pills: `.myfiPillTiny` (also has `.myfiPill`)

Goal:
- Make reward pills clear, compact, and consistent with baseline tokens/base.
